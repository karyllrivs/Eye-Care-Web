import { userSlice } from "./user.reducer";
export const { setCurrentUser } = userSlice.actions;
export const { setCurrentAdmin } = userSlice.actions;
export const { setCurrentPersonnel } = userSlice.actions;
